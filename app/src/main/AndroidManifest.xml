<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andoridproject">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key"/>

        <activity android:name=".MapsClass"></activity>
        <activity android:name=".DisplayProgram" />
        <activity android:name=".FoodTips_Json" />
        <activity android:name=".UpdateUsers" />
        <activity android:name=".MainActivityUsers" />
        <activity android:name=".DeleteAccounts" />
        <activity android:name=".DisplayUsers" />
        <activity android:name=".ListActivity" />
        <activity android:name=".AddForm" />
        <activity android:name=".Register_Form" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login_Form">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>